#!/usr/bin/env bash
set -euo pipefail

# Repo-local get wrapper. Available on PATH after `source START_HERE.sh`.
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/_wrapper_common.sh"
ts_wrapper_bootstrap "${BASH_SOURCE[0]}"
exec "$TAGSLUT_TOOLS_DIR/get" "$@"
