# Legacy Modules

This directory holds legacy modules that are no longer part of the active V2 flow.
They are kept for reference or migration support.

- `library.py`: Legacy path-based zone helpers. Replaced by `tagslut/utils/zones.py` and `ZoneManager`.

If you need any of these features, prefer the modern equivalents documented in `docs/ZONES.md`.
