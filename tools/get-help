#!/usr/bin/env bash

echo "Primary command:"
echo "1) tools/get <provider-url> - precheck + download + local tag prep + promote + m3u"
echo "2) tools/get-report <beatport-url> - report only, no download"
echo ""
echo "Advanced/internal:"
echo "3) tools/get-intake ... - process an existing batch root or run m3u-only flows"
echo "4) tools/get-sync <beatport-url> - deprecated compatibility alias for tools/get"
echo ""
echo "Also supported providers:"
echo "5) tools/get <tidal-url> - same umbrella workflow via tiddl"
echo "6) tools/get <deezer-url> - download via deemix + auto-register (source=deezer)"
echo ""
echo "Mode flags for both Beatport and Tidal:"
echo "  --sync / --synch : sync workflow"
echo "  --simple         : downloader-only behavior"
echo ""
echo "Useful workflow flags:"
echo "  --dj             : create DJ MP3 copies after promote"
echo "  --hoard          : local tag prep before promote; enrich/art continue in background after promote (default)"
echo "  --no-hoard       : skip local tag prep and background enrich/art"
echo "  --no-precheck    : skip duplicate-quality precheck"
echo "  --verbose        : print internal paths and detailed stage diagnostics"
echo ""
echo "Work roots:"
echo "  FIX_ROOT         salvageable metadata/tag issues"
echo "  QUARANTINE_ROOT  risky files only"
echo "  DISCARD_ROOT     deterministic duplicates like dest_exists"
