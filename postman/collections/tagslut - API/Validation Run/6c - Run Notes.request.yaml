$kind: http-request
description: Placeholder request used to capture inline notes, run-specific
  observations, and test findings during the validation workflow. Not a real API
  call. Used to document step-by-step outcomes alongside the other validation
  requests in this folder.
url: https://example.com
method: GET
auth:
  type: noauth
order: 3000
