#!/usr/bin/env bash
set -euo pipefail

# Repo-local enrich wrapper. Available on PATH after `source START_HERE.sh`.
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec poetry -C "$REPO_ROOT" run python -m tagslut enrich "$@"
