#!/usr/bin/env bash

echo "Use one of these 3 commands:"
echo "1) tools/get <beatport-url> - download missing + build playlist"
echo "2) tools/get-sync <beatport-url> - same flow, explicit sync"
echo "3) tools/get-report <beatport-url> - report only, no download"
echo ""
echo "Also supported:"
echo "4) tools/get <tidal-url> - download via tiddl wrapper"
echo "5) tools/get <deezer-url> - download via deemix + auto-register (source=deezer)"
echo ""
echo "Mode flags for both Beatport and Tidal:"
echo "  --sync / --synch : sync workflow"
echo "  --simple         : downloader-only behavior"
