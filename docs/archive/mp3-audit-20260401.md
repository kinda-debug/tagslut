<!-- Status: Active audit output captured from current execution environment on 2026-04-01. -->

# MP3 Stray Asset Audit — 2026-04-01

## Scope
- Canonical MP3 root: `/Volumes/MUSIC/MP3_LIBRARY`
- Audit target: MP3 files outside canonical root

## Task 0a — Full inventory (outside `MP3_LIBRARY`)
Command:

```bash
find /Volumes/MUSIC \
  -type f -iname '*.mp3' \
  ! -path '/Volumes/MUSIC/MP3_LIBRARY/*' \
  | sed 's#^/Volumes/MUSIC/##' \
  | sort
```

Observed output:

```text
find: ‘/Volumes/MUSIC’: No such file or directory
```

## Task 0b — Count by known area
Command:

```bash
echo "_work: $(find /Volumes/MUSIC/_work -type f -iname '*.mp3' | wc -l | tr -d ' ')"
echo "mdl:   $(find /Volumes/MUSIC/mdl   -type f -iname '*.mp3' | wc -l | tr -d ' ')"
echo "other: $(find /Volumes/MUSIC -type f -iname '*.mp3' \
  ! -path '/Volumes/MUSIC/MP3_LIBRARY/*' \
  ! -path '/Volumes/MUSIC/_work/*' \
  ! -path '/Volumes/MUSIC/mdl/*' | wc -l | tr -d ' ')"
```

Observed output:

```text
find: ‘/Volumes/MUSIC/_work’: No such file or directory
_work: 0
find: ‘/Volumes/MUSIC/mdl’: No such file or directory
mdl:   0
find: ‘/Volumes/MUSIC’: No such file or directory
other: 0
```

## Task 0c — Top folders with stray MP3s
Command:

```bash
find /Volumes/MUSIC \
  -type f -iname '*.mp3' \
  ! -path '/Volumes/MUSIC/MP3_LIBRARY/*' \
  -print0 \
| xargs -0 -n1 dirname \
| sed 's#^/Volumes/MUSIC/##' \
| cut -d/ -f1-2 \
| sort | uniq -c | sort -nr
```

Observed output:

```text
find: ‘/Volumes/MUSIC’: No such file or directory
dirname: missing operand
Try 'dirname --help' for more information.
```

## Task 0d — Decision per area
The target volume `/Volumes/MUSIC` is not mounted in this execution environment, so no folder groups could be enumerated from ground truth file data.

| Area group | Classification | Notes |
|---|---|---|
| `/Volumes/MUSIC` (entire scope) | `UNKNOWN` | Volume missing; manual rerun required on host with MUSIC volume mounted. |

## Next operator action
Re-run this exact audit on the host that has `/Volumes/MUSIC` mounted, then update this report with concrete folder-level `CONSOLIDATE` / `SOURCE_ONLY` / `UNKNOWN` decisions.
